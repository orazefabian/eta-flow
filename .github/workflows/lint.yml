name: Lint

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  lint:
    name: ESLint, Prettier, Type check & Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

      - name: Verify committed bundle is up to date
        run: |
          if ! git diff --quiet -- dist/eta-flow-card.js; then
            echo "::error::dist/eta-flow-card.js is stale. Run 'npm run build' and commit the result."
            git --no-pager diff --stat -- dist/eta-flow-card.js
            exit 1
          fi
